# Post-processing pipeline dependencies (top-level packages only).
# Transitive sub-dependencies are pulled in automatically and not listed here.
# Segmentation inference (torch / mmcv / mmseg / DCNv3) uses a separate environment.
numpy==2.2.6
opencv-contrib-python==4.11.0.86
pandas==2.3.2
pycocotools==2.0.10
shapely==2.1.1
tqdm==4.67.3
